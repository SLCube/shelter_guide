plugins {
    id 'java'
}

bootJar.enabled = false
repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.springdoc:springdoc-openapi-ui:1.6.6'	// web
    implementation 'org.springframework.boot:spring-boot-starter-web'	// web
}

test {
    useJUnitPlatform()
}