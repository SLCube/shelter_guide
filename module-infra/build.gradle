plugins {
    id 'java'
}

group = 'com.slcube'
version = 'unspecified'

repositories {
    mavenCentral()
}

dependencies {
    api project(':module-domain')
    implementation 'com.h2database:h2'
    runtimeOnly 'com.mysql:mysql-connector-j'
}

test {
    useJUnitPlatform()
}